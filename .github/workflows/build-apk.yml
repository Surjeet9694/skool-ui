name: Build Flutter APK

on:
  push:
    branches: [main]
  workflow_dispatch:   # allows manual trigger from GitHub UI

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.0'
          channel: stable

      - name: Create .env file
        run: |
          echo "APP_NAME=Skool" > .env
          echo "APP_ENV=production" >> .env
          echo "API_BASE_URL=${{ secrets.API_BASE_URL }}" >> .env
          echo "CONNECTION_TIMEOUT_MS=15000" >> .env
          echo "RECEIVE_TIMEOUT_MS=30000" >> .env

      - name: Generate Android platform files
        run: |
          # Save our pubspec.yaml before flutter create overwrites it
          cp pubspec.yaml pubspec.yaml.bak

          # Generate missing android/ (and other platform) folders
          flutter create --platforms android --project-name skool_ui .

          # Restore our original pubspec.yaml (flutter create overwrites it)
          cp pubspec.yaml.bak pubspec.yaml

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: skool-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 7
